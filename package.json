{
  "name": "com-fs-utils-lib",
  "version": "1.0.0",
  "description": "Util is a collection of general purpose gRPC models",
  "main": "models/build/index.js",
  "scripts": {
    "build": "rm -rf ./build && npx tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.8.12",
    "protoc-gen-ts": "^0.8.5",
    "ts-proto": "^1.141.0"
  }
}
